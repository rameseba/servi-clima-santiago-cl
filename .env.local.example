# Copia este archivo a .env.local y completa con tu deployment de Convex.
# La URL la entrega Convex al crear/abrir el deployment (Dashboard o `npx convex dev`).
VITE_CONVEX_URL=https://TU-DEPLOYMENT.convex.cloud
